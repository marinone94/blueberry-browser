/**
 * IPC Infrastructure Module
 * 
 * This module provides the foundation for feature-based IPC handler organization.
 * It replaces the monolithic EventManager pattern with a modular approach.
 */

export { BaseIPCHandler } from "./BaseIPCHandler";
export { IPCRegistry } from "./IPCRegistry";

